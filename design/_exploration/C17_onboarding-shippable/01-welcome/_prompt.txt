Professional product screenshot of a macOS 26 desktop showing the Yinghua (映话) app FIRST-LAUNCH WELCOME window, captured as a marketing-quality product screenshot. This is step 1 of a 3-step first-launch onboarding flow. All UI text in the rendered window MUST be in Simplified Chinese (real word combinations, no gibberish, no English).

## BASE macOS LAYER (IDENTICAL across all 3 onboarding screens)

The desktop wallpaper is a real macOS-style dark aurora over a starry sky — predominantly deep purple at left transitioning to teal/cyan at right, with subtle aurora ribbon curves and faint star points.

Menu bar (top, black frosted glass): translucent black bar. Left side in WHITE text: Apple logo, then "Yinghua", then "File  Edit  View  Window  Help" (the active app name MUST be "Yinghua", not "Finder"). Right side status icons in white: Control Center, Battery, Wi-Fi, Search, and clock reading EXACTLY "21:42". Do NOT add any other menu items.

Dock (bottom center, glass with subtle blur): EXACTLY these 12 elements in this EXACT order (must match the same layout used in screens 2 and 3 of this same onboarding flow):
1. Finder (blue-white face)
2. Safari (compass blue)
3. Messages (green speech bubble)
4. Mail (blue envelope)
5. Notes (YELLOW notepad — the iconic macOS yellow lined-paper notepad)
6. Calendar (white page with the number "26" in red, and a real weekday "MON" in small grey text below "26" — NOT gibberish, NOT random letters, NOT Cyrillic)
7. Reminders (white list with red/yellow/green dots)
8. Maps (map with roads)
9. Music (red note on white)
10. System Settings (gray gear)
11. A small separator dot
12. The Yinghua app icon (purple-to-teal aurora Y on dark glass, with a small magenta active dot below indicating the app is running)

DO NOT add Contacts, Photos, FaceTime, Trash, Launchpad, App Store, TV, or any other icon. DO NOT change the order.

## APP WINDOW — WELCOME (centered, ~720px wide, taller than wide)

A single macOS window (NOT multiple windows) with three standard traffic light buttons (red close, yellow minimize, green maximize) on the upper left. The window is centered horizontally on the screen, with dark glass material (vibrancy) and 14px corner radius. The window height is about 820-880px.

### CONTENT (centered, vertical stack, top to bottom)

- **Top 3-dot progress indicator**: a horizontal 3-dot progress indicator centered at the top of the content area (~32px below the title bar). Dot 1 (left) is a LARGER SOLID PURPLE FILLED circle (12px diameter) with a subtle purple glow halo around it — ACTIVE step. Dot 2 (center) is a small solid purple filled circle (8px diameter) — pending. Dot 3 (right) is a small HOLLOW GRAY circle (8px diameter, 1px gray border) — pending. A thin purple connecting line runs between dot 1 and dot 2.

- **Brand mark**: A 96x96 squircle with 22% corner radius, dark glass surface with a subtle aurora gradient inside, containing the Yinghua Y mark in white (a stylized Y letterform, not a chat bubble, not a circle pair). NO glow, NO halo, NO sparkles around it.

- **Brand name "映话"**: Render the Chinese characters "映话" in large SF Pro Display-style font, weight 600, warm white (#F4F1EC), ~30pt. Centered.

- **Subtitle line "为面试而生的 macOS 智能助手"**: A single line in warm white at 70% opacity, SF Pro Text, weight 400, ~14pt, centered. This is a real Chinese phrase meaning "macOS smart assistant built for interviews" — render the characters as a real word combination, NOT fragments.

- **3 feature bullets** (centered, generous spacing ~16px between rows, no icons, just small purple dots then text):
  - "系统级录音 + 麦克风，全程本地"
  - "实时转录，自动分说话人"
  - "AI 总结、决定、待办"
  Each row starts with a tiny purple dot (4px) on the left, then the Chinese text in warm white at 85% opacity, SF Pro Text ~13pt.

- **Primary CTA button**: A wide purple-to-teal gradient button (vivid purple #B57BFF top-left to teal #2DD4BF bottom-right, diagonal), ~360px wide, ~48px tall, 12px corner radius, centered. Label "开始使用 ↗" in white SF Pro Text weight 600 ~15pt. This is the ONLY primary button. The arrow icon is a small ↗ glyph after the text.

- **Ghost link below CTA**: "已有账号" in muted gray (~60% white) ~12pt, no underline, centered.

## STRICT RULES (must follow)

- The base macOS layer (menu bar with "Yinghua" app name, Dock with 12 elements in the locked order, aurora wallpaper, clock 21:42) must be IDENTICAL across this screen and the next two onboarding screens — do NOT invent a different wallpaper or dock.
- This is a MINIMAL welcome window. Do NOT add sidebar, toolbar with multiple icons, search box, or preferences.
- NO waveform, NO EKG, NO sine wave anywhere.
- NO cyberpunk glow, NO neon outlines, NO holographic effects, NO "futuristic" decoration.
- NO sparkles decoration around the brand mark. NO halo. NO glow ring (except the tiny progress dot 1 glow which is required).
- Use the same purple-to-teal gradient for the CTA as the Yinghua brand Y.
- All visible UI text inside the window MUST be the Simplified Chinese listed above, rendered as real word combinations. Do NOT translate to English. Do NOT use lorem ipsum. Do NOT use character fragments. Do NOT render any English UI labels inside the app window.
- Window surface: dark glass with aurora-tinted vibrancy, ~70% opacity, 14px corner radius.
- Real macOS feel: looks like a real SwiftUI welcome sheet, not a web landing page overlay.
- Aspect ratio 16:9, 4K resolution.

## STRICT RULES (anti-leak addendum)

- Do NOT render any text that appears inside this prompt itself. Specifically forbidden visible text: any font-size, font-weight, hex color, "STYLE 1", "STYLE 2", "vibrancy", "regularMaterial", "regular material", "AI", "node", "TEST", "Sample", "Placeholder", "SF Pro", "JetBrains Mono", "~14pt regular", "600 15pt", "graphite", "65%", "80%", or any design-system metadata.
- Do NOT render brand names as icon glyphs. The Yinghua Y in the brand mark is a stylized letter, not a chat icon, not a sparkle, not a circle pair.
- Do NOT invent labels for app menu names, status bars, or dock indicators beyond what is explicitly listed above. If unsure, leave the area empty.
- The Dock calendar app icon MUST show "26" in red AND a real weekday ("MON" or "TUE" or "WED" or "THU" or "FRI" or "SAT" or "SUN") in small grey text below. Do NOT render gibberish like "NEN", "MEM", "MIY", "MEN", "ПАН" or Cyrillic characters.
- The Dock 5th icon MUST be the macOS Notes app (yellow notepad). Do NOT render Contacts, Photos, or any other app in position 5.

Output: a single product-quality screenshot, no text overlays, no watermark, no captions, no prompt artifacts.
