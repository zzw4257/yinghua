Professional product screenshot of a macOS 26 desktop showing the Yinghua (映话) app SECOND-STEP onboarding window (PERMISSION REQUEST), captured as a marketing-quality product screenshot. This is step 2 of a 3-step first-launch flow. All UI text in the rendered window MUST be in Simplified Chinese (real word combinations, no gibberish, no English).

## BASE macOS LAYER (IDENTICAL across all 3 onboarding screens)

The desktop wallpaper is a real macOS-style dark aurora over a starry sky — predominantly deep purple at left transitioning to teal/cyan at right, with subtle aurora ribbon curves and faint star points. The wallpaper MUST be visually consistent with screen 1 (welcome) and screen 3 (byok) of this same onboarding flow.

Menu bar (top, black frosted glass): translucent black bar. Left side in WHITE text: Apple logo, then "Yinghua", then "File  Edit  View  Window  Help" (the active app name MUST be "Yinghua", not "Finder"). Right side status icons in white: Control Center, Battery, Wi-Fi, Search, and clock reading EXACTLY "21:42". Do NOT add any other menu items.

Dock (bottom center, glass with subtle blur): EXACTLY these 12 elements in this EXACT order (must match screens 1 and 3):
1. Finder (blue-white face)
2. Safari (compass blue)
3. Messages (green speech bubble)
4. Mail (blue envelope)
5. Notes (YELLOW notepad — the iconic macOS yellow lined-paper notepad)
6. Calendar (white page with the number "26" in red, and a real weekday "TUE" in small grey text below "26" — NOT gibberish, NOT random letters, NOT Cyrillic)
7. Reminders (white list with red/yellow/green dots)
8. Maps (map with roads)
9. Music (red note on white)
10. System Settings (gray gear)
11. A small separator dot
12. The Yinghua app icon (purple-to-teal aurora Y on dark glass, with a small magenta active dot below indicating the app is running)

DO NOT add Contacts, Photos, FaceTime, Trash, Launchpad, App Store, TV, or any other icon. DO NOT change the order.

## APP WINDOW — PERMISSION REQUEST (centered, ~720px wide, taller than wide)

A single macOS window (NOT multiple windows) with three standard traffic light buttons (red close, yellow minimize, green maximize) on the upper left. The window is centered horizontally on the screen, with dark glass material (vibrancy) and 14px corner radius. The window width is approximately 720px (matches screen 1 and screen 3 of this onboarding flow).

### CONTENT (vertical stack, top to bottom)

- **Top 3-dot progress indicator**: a horizontal 3-dot progress indicator centered at the top of the content area (~32px below the title bar). Dot 1 (left) is a small solid purple filled circle (8px diameter) — step 1 complete. Dot 2 (center) is a LARGER SOLID PURPLE FILLED circle (12px diameter) with a subtle purple glow halo around it — ACTIVE step. Dot 3 (right) is a small HOLLOW GRAY circle (8px diameter, 1px gray border) — pending. Thin purple connecting lines run between dot 1 and dot 2, and between dot 2 and dot 3.

- **Header section** (~32px below the progress dots):
  - Heading "授权访问" in SF Pro Display, weight 600, warm white (#F4F1EC), ~26pt, left-aligned with ~32px horizontal padding from window edges.
  - Subheading "映话需要以下 macOS 权限才能录制和转录你的会议" in SF Pro Text, weight 400, ~14pt, muted gray (~70% white), left-aligned, same horizontal padding. Real Chinese phrase, no fragments.

- **3 permission cards** (vertical stack, ~12px gap between cards, ~32px horizontal padding from window edges):
  Each card is a horizontal row, ~72px tall, dark glass material with 1px white 8% hairline border, 12px corner radius. Layout: left 32x32 icon area, middle text area (flex), right status pill area.

  **Card 1 — 麦克风 (REQUIRED, GRANTED)**
  - Left: 32x32 dark glass squircle with a small microphone SF Symbol-style icon (white) inside.
  - Middle: bold title "麦克风" in warm white ~14pt 600, below it a one-line description "录制系统音频 + 麦克风" in muted gray ~12pt 400.
  - Right: small status pill, ~80x24px, rounded capsule. Background soft green tinted glass (#34C759 at 20% opacity), text "已授权" in green #34C759 ~11pt 600.

  **Card 2 — 屏幕录制 (REQUIRED, PENDING)**
  - Left: 32x32 dark glass squircle with a small screen-recording SF Symbol-style icon (white) — a rectangle with a filled circle inside.
  - Middle: bold title "屏幕录制" in warm white ~14pt 600, below it "捕获应用和会议的系统音频" in muted gray ~12pt 400.
  - Right: small status pill, ~80x24px. Background dark gray glass, text "待授权" in muted gray ~11pt 600.

  **Card 3 — 通知 (OPTIONAL, PENDING)**
  - Left: 32x32 dark glass squircle with a small bell SF Symbol-style icon (white) inside.
  - Middle: bold title "通知" in warm white ~14pt 600, below it "会后温和提醒" in muted gray ~12pt 400.
  - Right: small status pill, ~80x24px. Background dark gray glass, text "可选" in muted gray ~11pt 600.

- **Footer section** (~32px below the last card):
  - Primary CTA: A wide purple-to-teal gradient button, ~360px wide, ~44px tall, 12px corner radius, centered horizontally. Vivid purple #B57BFF top-left to teal #2DD4BF bottom-right diagonal gradient. Label "继续" in white SF Pro Text weight 600 ~14pt. NO arrow icon. This is the ONLY primary button in the window.
  - Below the CTA, a small ghost link: "稍后再说" in muted gray ~12pt, no underline, centered.

## STRICT RULES (must follow)

- The base macOS layer (menu bar with "Yinghua" app name, Dock with 12 elements in the locked order, aurora wallpaper, clock 21:42) must be IDENTICAL across this screen and screens 1 and 3 of this onboarding flow — do NOT invent a different wallpaper or dock.
- The 3-dot progress indicator MUST be at the TOP of the window content, NOT the bottom. Dot 2 is the active step (larger + purple + glow).
- All status pills use SOLID COLOR TEXT. Use system colors: green #34C759 for granted, gray for pending/optional.
- Card 1 status MUST be "已授权" (green pill). Cards 2 and 3 are gray pills ("待授权" and "可选").
- NO waveform, NO EKG, NO sine wave anywhere.
- NO cyberpunk glow, NO neon outlines, NO holographic effects, NO "futuristic" decoration.
- NO sparkles decoration. NO halo. NO glow ring (except the tiny progress dot 2 glow which is required).
- All visible UI text inside the window MUST be the Simplified Chinese listed above, rendered as real word combinations. Do NOT translate to English. Do NOT use lorem ipsum. Do NOT use character fragments.
- The primary CTA uses the same purple-to-teal gradient as the Yinghua brand Y.
- Window surface: dark glass with aurora-tinted vibrancy, ~70% opacity, 14px corner radius.
- Real macOS feel: looks like a real SwiftUI window, not a web landing page overlay.
- Single window only. NO additional panels, NO floating control panels, NO sidebar.
- Aspect ratio 16:9, 4K resolution.

## STRICT RULES (anti-leak addendum)

- Do NOT render any text that appears inside this prompt itself. Specifically forbidden visible text: any font-size, font-weight, hex color, "STYLE 1", "STYLE 2", "vibrancy", "regularMaterial", "regular material", "AI", "node", "TEST", "Sample", "Placeholder", "SF Pro", "JetBrains Mono", "~14pt regular", "600 15pt", "graphite", "65%", "80%", "8%", "1px", "12px", "14px", "26pt", "44px", "360px", "72px", "32x32", or any design-system metadata.
- Do NOT render brand names as icon glyphs. The icon for the provider or any other concept tile must use abstract geometric shapes, never letters or words.
- Do NOT invent labels for app menu names, status bars, or dock indicators beyond what is explicitly listed above. If unsure, leave the area empty.
- The Dock calendar app icon MUST show "26" in red AND a real weekday ("MON" or "TUE" or "WED" or "THU" or "FRI" or "SAT" or "SUN") in small grey text below. Do NOT render gibberish like "NEN", "MEM", "MIY", "MEN", "ПАН" or Cyrillic characters.
- The Dock 5th icon MUST be the macOS Notes app (yellow notepad). Do NOT render Contacts, Photos, or any other app in position 5.

Output: a single product-quality screenshot, no text overlays, no watermark, no captions, no prompt artifacts.
