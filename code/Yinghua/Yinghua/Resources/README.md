# Resources

App icon (placeholder) + accent color set live here.

- `Assets.xcassets/AppIcon.appiconset/` — placeholder 01 MINIMAL (black squircle + Y)
- `Assets.xcassets/AccentColor.colorset/` — 紫 vivid `#B57BFF`

The 10 icon PNGs are auto-generated by `Yinghua/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json`. Regenerate with `swift /tmp/gen_icons.swift` (or replace with a real design asset from `design/_exploration/C07_app-icon-v3/01-minimal-letterform/`).

Replace these placeholders before shipping:
- Real squircle + Y from C07/01 (V3 minimal letterform, 1024x1024)
- AccentColor values if design changes

The `YinghuaMark` SwiftUI view in `Views/MainWindow.swift` is the same Y mark rendered programmatically — used in the onboarding welcome screen.
